
.orderButton
  max-height x(60)
  width 100%
  height 100%
  background-color $green
  color $white
  font-size x(30)
  text-align center
  opacity 1
  transition opacity .3s ease-in-out

  &.disabled
    opacity 0.5
    cursor default
